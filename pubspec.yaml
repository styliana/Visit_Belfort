# pubspec.yaml
name: simple_belfort_app # Ensure this matches your project name
description: A simple app to discover attractions in Belfort.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # This declares your image assets
  assets:
    - assets/images/